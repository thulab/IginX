name: Cancel

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  cancel:
    timeout-minutes: 3
    runs-on: ubuntu-latest
    steps:
      - uses: styfle/cancel-workflow-action@0.9.1
        with:
          workflow_id: all